{
  "name": "agent-arena-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run script/deploy.ts --network botchainTestnet",
    "deploy:mainnet": "hardhat run script/deploy.ts --network botchain"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "solc": "^0.8.28"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
